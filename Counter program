# Counter Program

# Write a program that counts for the user. Let the user enter the
# starting number, the ending number, and the amount by which
# to count.

print ("Hey! welcome. I'm your counter, you are required to provide the starting number, the\
ending number and the amount by which to count.")
# first number
f_number=int(input("\nStarting number: "))

# second number
e_number=int(input("\nEding number: "))

# interval
interval=int(input("\nAmount to which to count."))
e_number = e_number + interval

for i in range(f_number, e_number, interval):
    print(i, end=" ")



input("\n\nPress the enter key to exit")
